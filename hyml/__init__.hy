#!/usr/bin/python3

; TODO: not working due to Hy bug / behaviour,
; must point directly to the files on the modules
; or use __init__.py for inclusion
;(require (hyml.macros (*)))
;(import (hyml.macros (*)))
;(require (hyml.helpers (*)))
;(import (hyml.helpers (println indent)))
